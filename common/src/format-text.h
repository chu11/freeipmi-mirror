#ifndef _FORMAT_TEXT_H
#define _FORMAT_TEXT_H

int format_text(char prefix, 
                int column_width,
                char *in, 
                char *out, 
                unsigned int outsize);
#endif
