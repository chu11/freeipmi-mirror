#! @prefix@/sbin/fish -s
!#
(catch 'misc-error
       (lambda ()
	(sel-main (fi-command-line)))
       (lambda (k args . opts)
	 (display "sel: error: ")
	 (display (cadr opts))
	 (newline)))
(fi-exit sel-exit-status)

