#! @prefix@/sbin/fish -s
!#
(catch 'misc-error
       (lambda ()
	(bmc-config-main (fi-command-line)))
       (lambda (k args . opts)
	 (display "bmc-config: error: ")
	 (display (cadr opts))
	 (newline)))
(fi-exit bmc-config-exit-status)

