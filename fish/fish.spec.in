# fish.spec
#
# Copyright (c) 2003 FreeIPMI Core Team
#
%define name fish
%define version @PACKAGE_VERSION@
%define release 0.alpha5.qa3

Name: %{name}
Version: %{version}
Release: %{release}
License: GPL
Group: Application/Misc
Source: %{name}-%{version}.tar.gz
BuildRoot: /var/tmp/%{name}-%{version}
Requires: libfreeipmi = 0.0.0-0.alpha5.qa3
Requires: guile >= 1.6.4-8
Requires: guile-devel >= 1.6.4-8 

Summary: Free IPMI SHell - an extensible console based shell for managing large number of IPMI compatible systems.

%description
Free IPMI SHell - an extensible console based shell for managing large number of IPMI compatible systems.

%prep
%setup -q

%build
./autogen.sh
%configure --program-prefix=%{?_program_prefix:%{_program_prefix}}
make

%install
rm -rf $RPM_BUILD_ROOT
mkdir -p $RPM_BUILD_ROOT
%makeinstall

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root)
%doc AUTHORS ChangeLog COPYING INSTALL NEWS README
/usr/sbin
/usr/share
/etc

%changelog
