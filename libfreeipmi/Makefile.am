SUBDIRS = include src 

CLEANFILES = 
DISTCLEANFILES = .deps/*.P

# Semaphore IPC Key file for ftok:
IPCKEY = ipckey

IPCDIR = /var/lib/$(PACKAGE)

Ipckeydir     = $(IPCDIR)
Ipckey_DATA   = $(IPCKEY)

EXTRA_DIST = $(IPCKEY)

