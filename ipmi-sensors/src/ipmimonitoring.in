#!/bin/sh

options=$@

if [[ "$options" =~ "--legacy-output" ]]; then
    options=${options/"--legacy-output"/"--ipmimonitoring-legacy-output"}
fi

exec @IPMIMONITORINGSBINDIR@/ipmi-sensors --output-sensor-state ${options}
